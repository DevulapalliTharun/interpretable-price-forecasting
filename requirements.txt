# ── Core ML ────────────────────────────────────────────────────────────────
# PyTorch: install the GPU build if you have CUDA, otherwise use the CPU build.
#
#   GPU (CUDA 12.8, tested):
#     pip install torch==2.11.0+cu128 --index-url https://download.pytorch.org/whl/cu128
#
#   CPU-only (for reviewers without a GPU):
#     pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cpu
#
# After installing torch, install the rest with:
#     pip install -r requirements.txt

pytorch-forecasting>=1.0,<2.0
pytorch-lightning>=2.0,<3.0
pytorch_optimizer>=3.0

# ── Data and ML ────────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.26
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3
scipy>=1.11

# ── Visualisation and dashboard ────────────────────────────────────────────
plotly>=5.18
streamlit>=1.30
kaleido==0.2.1
matplotlib>=3.8

# ── Data collection ────────────────────────────────────────────────────────
requests>=2.31
gnews>=0.4.0
nltk>=3.8

# ── Utilities ──────────────────────────────────────────────────────────────
tqdm>=4.65
